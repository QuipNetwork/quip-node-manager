# GitLab CI/CD for Quip Node Manager
# Tauri v2 desktop app builds for macOS (arm64 + x86_64), Linux, Windows

stages:
  - build
  - release

# ---------------------------------------------------------------------------
# Shared setup
# ---------------------------------------------------------------------------

.tauri_build:
  stage: build
  cache:
    key: "${CI_JOB_NAME}"
    paths:
      - src-tauri/target
  artifacts:
    expire_in: 30 days
  rules:
    - if: $CI_COMMIT_TAG
    - if: $CI_COMMIT_BRANCH

# ---------------------------------------------------------------------------
# macOS ARM64 (Apple Silicon)
# ---------------------------------------------------------------------------

build-macos-arm64:
  extends: .tauri_build
  tags: [macos, arm64]
  before_script:
    - export PATH="/opt/homebrew/opt/rustup/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$HOME/.bun/bin:$HOME/.cargo/bin:$PATH"
    - rustup default stable 2>/dev/null || true
    - curl -fsSL https://bun.sh/install | bash || true
    - bun install
  script:
    - export PATH="/opt/homebrew/opt/rustup/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$HOME/.bun/bin:$HOME/.cargo/bin:$PATH"
    - bun run tauri build
  artifacts:
    paths:
      - src-tauri/target/release/bundle/dmg/*.dmg
      - src-tauri/target/release/bundle/macos/*.app

# ---------------------------------------------------------------------------
# macOS x86_64 (Intel)
# ---------------------------------------------------------------------------

build-macos-x86_64:
  extends: .tauri_build
  tags: [macos, x86_64]
  before_script:
    - export PATH="/usr/local/opt/rustup/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$HOME/.bun/bin:$HOME/.cargo/bin:$PATH"
    - rustup default stable 2>/dev/null || true
    - curl -fsSL https://bun.sh/install | bash || true
    - bun install
  script:
    - export PATH="/usr/local/opt/rustup/bin:/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin:/usr/sbin:/sbin:$HOME/.bun/bin:$HOME/.cargo/bin:$PATH"
    - bun run tauri build
  artifacts:
    paths:
      - src-tauri/target/release/bundle/dmg/*.dmg
      - src-tauri/target/release/bundle/macos/*.app

# ---------------------------------------------------------------------------
# Linux x86_64 (AppImage + deb)
# ---------------------------------------------------------------------------

build-linux-x86_64:
  extends: .tauri_build
  image: ubuntu:22.04
  tags: [linux, x86_64]
  before_script:
    # System deps for Tauri on Linux
    - apt-get update && apt-get install -y --no-install-recommends
        build-essential curl file git ca-certificates
        libwebkit2gtk-4.1-dev libgtk-3-dev libayatana-appindicator3-dev
        librsvg2-dev patchelf pkg-config libssl-dev
    # Install Rust via rustup (latest stable, satisfies 1.88+ requirement)
    - curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
    - . "$HOME/.cargo/env"
    # Install bun
    - curl -fsSL https://bun.sh/install | bash
    - export PATH="$HOME/.bun/bin:$PATH"
    - bun install
  script:
    - . "$HOME/.cargo/env"
    - export PATH="$HOME/.bun/bin:$PATH"
    - bun run tauri build
  artifacts:
    paths:
      - src-tauri/target/release/bundle/appimage/*.AppImage
      - src-tauri/target/release/bundle/deb/*.deb

# ---------------------------------------------------------------------------
# Windows x86_64 (NSIS installer)
# ---------------------------------------------------------------------------

build-windows-x86_64:
  extends: .tauri_build
  tags: [windows, x86_64]
  before_script:
    - $env:Path = "C:\ProgramData\chocolatey\bin;C:\Program Files\nodejs;$env:USERPROFILE\.cargo\bin;$env:Path"
    - choco install nodejs-lts rust-ms -y --no-progress
    - $env:Path = "C:\ProgramData\chocolatey\bin;C:\Program Files\nodejs;$env:USERPROFILE\.cargo\bin;$env:Path"
    - npm install -g bun
    - bun install
  script:
    - $env:Path = "C:\ProgramData\chocolatey\bin;C:\Program Files\nodejs;$env:USERPROFILE\.cargo\bin;$env:USERPROFILE\.bun\bin;$env:Path"
    - bun run tauri build
  artifacts:
    paths:
      - src-tauri/target/release/bundle/nsis/*.exe
      - src-tauri/target/release/bundle/msi/*.msi

# ---------------------------------------------------------------------------
# Release (tags only)
# ---------------------------------------------------------------------------

release-binaries:
  stage: release
  needs:
    - build-macos-arm64
    - build-macos-x86_64
    - build-linux-x86_64
    - build-windows-x86_64
  rules:
    - if: $CI_COMMIT_TAG
  script:
    - echo "Creating release $CI_COMMIT_TAG"
  release:
    tag_name: $CI_COMMIT_TAG
    description: "Release $CI_COMMIT_TAG"
    assets:
      links:
        - name: "macOS ARM64 (Apple Silicon) DMG"
          url: "${CI_PROJECT_URL}/-/jobs/artifacts/${CI_COMMIT_TAG}/download?job=build-macos-arm64"
          filepath: /quip-node-manager-macos-arm64.zip
        - name: "macOS x86_64 (Intel) DMG"
          url: "${CI_PROJECT_URL}/-/jobs/artifacts/${CI_COMMIT_TAG}/download?job=build-macos-x86_64"
          filepath: /quip-node-manager-macos-x86_64.zip
        - name: "Linux x86_64 (AppImage + deb)"
          url: "${CI_PROJECT_URL}/-/jobs/artifacts/${CI_COMMIT_TAG}/download?job=build-linux-x86_64"
          filepath: /quip-node-manager-linux-x86_64.zip
        - name: "Windows x86_64 (NSIS + MSI)"
          url: "${CI_PROJECT_URL}/-/jobs/artifacts/${CI_COMMIT_TAG}/download?job=build-windows-x86_64"
          filepath: /quip-node-manager-windows-x86_64.zip
